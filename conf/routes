# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                           controllers.Application.index()
POST    /bomb/:partidaid/:userid/:column/:row       controllers.Application.bomb(partidaid; String,
                                                                                 userid: String,
                                                                                 column: Int,
                                                                                 row: Int)
